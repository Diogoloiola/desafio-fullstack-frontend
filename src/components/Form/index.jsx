import {Container, Input, Button} from "./style";

export default function Form(){
    return (
        <Container>
            <Input />
            <Button>Search</Button>
        </Container>
    )
}